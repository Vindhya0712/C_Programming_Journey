#include <stdio.h>


int main() {
    int i = 1;
    printf("Increase, then use -- %d \n", i++);
    printf("Just use -- %d \n", i);
    printf("Use, then increase -- %d \n", ++i);   

    return 0;
}
